####################### Interface Information: ###############################
# Here you can configure the interfaces,  which is goning to be handled      #
# by the NTRT server. The general section contains the information            #
# necessary for the further interpretation. These are the following:         #
#         									                                 #
# inf_num = [VALUE], where the value determines the number of                #
# interfaces we want to configure.                                           #
#                                                                            #
# cmdport_local = [VALUE], where the value determines the portnum            #
# we use to receive commands from a remote NTRT_srv or client                 #
#									                                    	 #
# acceptance = [VALUE], where the value determines the acceptance            #
# of a remote new connection request                                         #
############################################################################## 

[general]
tunnel_num    = 1
interface_num = 2
#Accept remote new connection request
accept_remote = 0

[tun_0]
name      = tun0
ipv4_addr = 10.2.2.3/24
ipv6_addr = fd00:ab:203::1/64
cmdport_local = 65050

[interface_0]
name      = eth0:0
ipv4_addr = 10.1.4.3/24
ipv6_addr = fec0:1:103::2/64 


[interface_1]
name      = eth0:1
ipv4_addr = 10.1.5.3/24
ipv6_addr = fec0:1:113::2/64 
